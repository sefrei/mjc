{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des meetings</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for meeting in meetings %}
            <tr>
                <td><a href="{{ path('meeting_show', { 'id': meeting.id }) }}">{{ meeting.id }}</a></td>
                <td>{{ meeting.name }}</td>
                <td>{% if meeting.date %}{{ meeting.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a class="btn btn-primary" href="{{ path('meeting_show', { 'id': meeting.id }) }}">voir</a>
                        </li>
                        <li>
                            <a class="btn btn-warning" href="{{ path('meeting_edit', { 'id': meeting.id }) }}">modifier</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a class="btn btn-new" href="{{ path('meeting_new') }}">Créer un nouveau meeting</a>
        </li>
    </ul>
{% endblock %}
