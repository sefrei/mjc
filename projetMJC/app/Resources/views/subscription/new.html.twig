{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nouvelle inscription</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer l'inscription" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('subscription_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
