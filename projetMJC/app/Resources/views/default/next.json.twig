{
  "nextDayActivities" : [
  {% for item in result %}
    {
    "id" : {{item.id}},
    "date" : "{{item.startAt | date('Y-m-d h:i:s')}}",
    "nbActivity" :{{item.nblesson}}
    }
    {% if (loop.length == loop.index ) or (loop.last == true)  %} {% else %},{% endif %}
  {% endfor %}
  ]
}
